#include <bits/stdc++.h>

using namespace ::std;

typedef int64_t i64;

int main() {
  i64 tc;
  cin >> tc;
  while (tc--) {
    i64 a, b;
    cin >> a >> b;
    cout << (a / 3) * (b / 3) << endl;
  }
  return 0;
}